#!/bin/bash
# Install Fldigi from the official PPA (daily builds) or apt

set -euo pipefail
HAMAPPS_DIR="$(dirname "$(dirname "$(dirname "$(readlink -f "$0")")")")"
source "$HAMAPPS_DIR/scripts/utils"

info "Installing Fldigi..."
sudo apt-get install -y fldigi

success "Fldigi installed."
